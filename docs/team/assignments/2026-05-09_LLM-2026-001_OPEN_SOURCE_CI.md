# Assignment: Open Source CI And Contributor Basics

Employee ID: `LLM-2026-001`

Project role: `ROLE-API / ROLE-LLM-INTERFACE` temporarily assisting open-source
engineering.

Status: assigned

Assigned by: `LLM-2026-000`

Date: 2026-05-09

## Goal

Make the GitHub repository easier for outside contributors to verify and use.

## Scope

Own these files only unless a blocker requires a narrow related edit:

```text
.github/workflows/
.github/ISSUE_TEMPLATE/
CONTRIBUTING.md
README.md                  # only if needed to link contributor docs
docs/runbooks/
dev_logs/
docs/memory/handoffs/
```

## Required Work

1. Add a minimal GitHub Actions workflow that runs:

```text
python -m pip install -r requirements.txt
python -m unittest discover -s autonomous_crawler/tests
```

2. Add `CONTRIBUTING.md` with:

- setup summary
- test command
- no-secrets rule
- branch/PR guidance
- safety note for crawling targets

3. Add basic issue templates:

- bug report
- feature request
- crawl target/training report

4. Write a developer log.
5. Write a handoff note.

## Constraints

- Do not add dependencies.
- Do not require real API keys in CI.
- Do not enable browser smoke by default in CI.
- Do not change crawler runtime behavior.
- Do not edit files outside scope unless necessary and documented.

## Verification

Run locally:

```text
python -m unittest discover -s autonomous_crawler/tests
```

If you cannot run the full suite, explain why and run the smallest relevant
checks.

## Completion Report

Report:

- files changed
- tests run
- any CI limitations
- any docs still missing
