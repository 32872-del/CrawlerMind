# Assignment: CAP-2.1 JS Asset Inventory And Signature Clue MVP

## Assignee

Employee ID: `LLM-2026-002`

Project role: `ROLE-JS-RECON`

Status: assigned

Assigned by: `LLM-2026-000`

Date: 2026-05-12

## Capability IDs

```text
CAP-2.1 Frontend JS reverse engineering / AST foundation
CAP-2.2 Hook technique preparation
CAP-5.1 NLP-assisted selector/API reasoning support
```

## Goal

Implement a JS Asset Inventory MVP. This should help CLM identify which JS files
matter before deeper AST/hook work starts.

The first version can use deterministic Python parsing and regex heuristics.
Do not add Node/Babel dependencies yet unless you prove they are necessary.

## Why This Matters

Advanced crawler work often starts by locating:

- API endpoints hidden in JS
- signature/token/encryption functions
- anti-debug or challenge code
- source maps
- Webpack/Vite/Nuxt/Next bundles

This module should generate a concise "where to look next" report.

## Required Reading

Start with:

```text
git pull origin main
```

Then read:

```text
docs/plans/2026-05-12_CAPABILITY_IMPLEMENTATION_MATRIX.md
PROJECT_STATUS.md
autonomous_crawler/tools/html_recon.py
autonomous_crawler/tools/browser_network_observer.py
autonomous_crawler/tools/api_candidates.py
```

## Allowed Write Scope

You own:

```text
autonomous_crawler/tools/js_asset_inventory.py
autonomous_crawler/tests/test_js_asset_inventory.py
dev_logs/development/2026-05-12_HH-MM_cap_2_1_js_asset_inventory.md
docs/memory/handoffs/2026-05-12_LLM-2026-002_cap_2_1_js_asset_inventory.md
```

Do not edit browser interception code; worker 001 owns that. If you need input
from captured JS assets, design a function that accepts asset records as plain
dicts.

## Requirements

Implement functions/classes that can:

1. Extract script assets from HTML:
   - external script URLs
   - inline scripts
   - module/nomodule/type attributes
   - sourcemap hints
2. Analyze JS text for:
   - suspicious keywords: sign, signature, token, encrypt, crypto, nonce,
     captcha, wbi, x-bogus, verify, hmac, sha256
   - API endpoint strings
   - GraphQL strings
   - WebSocket URLs
   - sourcemap references
3. Return a ranked report:
   - asset URL or inline id
   - score
   - reasons
   - endpoint candidates
   - keyword hits
4. Deterministic tests with sample HTML/JS fixtures.

## Out Of Scope

- No real deobfuscation yet.
- No AST transform yet.
- No execution of untrusted JS.
- No site-specific hard-coding.

## Acceptance

Run:

```text
python -m unittest autonomous_crawler.tests.test_js_asset_inventory -v
python -m unittest autonomous_crawler.tests.test_api_intercept -v
python -m unittest discover -s autonomous_crawler/tests
```

Completion note must include:

- files changed
- tests run
- capability IDs covered
- how the output can feed future AST/Hook work
- remaining gaps
