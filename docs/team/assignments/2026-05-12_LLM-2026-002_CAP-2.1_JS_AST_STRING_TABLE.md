# Assignment: CAP-2.1 JS AST/String Table MVP

## Assignee

Employee ID: `LLM-2026-002`

Project role: `ROLE-JS-AST`

Status: assigned

Assigned by: `LLM-2026-000`

Date: 2026-05-12

## Capability IDs

```text
CAP-2.1 Frontend JS reverse engineering / AST foundation
CAP-2.2 Hook technique preparation
CAP-5.1 NLP-assisted API/selector reasoning
```

## Goal

Extend the current JS Asset Inventory into a lightweight JS analysis layer that
can identify useful strings and suspicious function/call clues before deeper
deobfuscation work.

Keep this deterministic and dependency-light. Do not introduce Node/Babel yet
unless you document why regex/token heuristics cannot satisfy this MVP.

## Required Reading

Start with:

```text
git pull origin main
```

Then read:

```text
docs/plans/2026-05-12_CAPABILITY_IMPLEMENTATION_MATRIX.md
PROJECT_STATUS.md
autonomous_crawler/tools/js_asset_inventory.py
autonomous_crawler/tests/test_js_asset_inventory.py
autonomous_crawler/tools/browser_interceptor.py
```

## Allowed Write Scope

You own:

```text
autonomous_crawler/tools/js_static_analysis.py
autonomous_crawler/tests/test_js_static_analysis.py
dev_logs/development/2026-05-12_HH-MM_cap_2_1_js_static_analysis.md
docs/memory/handoffs/2026-05-12_LLM-2026-002_cap_2_1_js_static_analysis.md
```

Avoid editing `js_asset_inventory.py` unless you only add a tiny integration
helper.

## Requirements

Implement functions/classes that can:

1. Extract string literals from JS text:
   - quoted strings
   - template literals without executing interpolation
   - URL/API-looking strings
2. Extract function/call clues:
   - function declarations
   - arrow/function assignment names
   - calls containing sign/token/encrypt/verify/captcha/fingerprint keywords
3. Produce a ranked static-analysis report:
   - string table count
   - endpoint-looking strings
   - suspicious function names
   - suspicious call names
   - reasons
   - score
4. Add deterministic tests for minified-ish JS, multiline JS, and harmless JS.

## Out Of Scope

- No execution of JS.
- No deobfuscation.
- No source-map download.
- No AST parser dependency unless approved.

## Acceptance

Run:

```text
python -m unittest autonomous_crawler.tests.test_js_static_analysis -v
python -m unittest autonomous_crawler.tests.test_js_asset_inventory -v
python -m unittest discover -s autonomous_crawler/tests
```

Completion note must include:

- files changed
- tests run
- capability IDs covered
- examples of extracted strings/functions/calls
- remaining gaps for AST/deobfuscation
